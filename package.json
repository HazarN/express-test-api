{
  "name": "express-test-api",
  "version": "1.0.0",
  "description": "A simple backend API with using PostgreSQL database",
  "main": "server.js",
  "scripts": {
    "start": "node --use_strict ./server.js",
    "dev": "nodemon --use_strict --exec \"cls && node\" ./server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.2"
  }
}
